Claude Code 强制开发规则（不可违反）
 
1. 架构强制规则
 
- 必须使用 6个独立Agent，严禁合并代码、合并prompt
- 每个Agent输出严格固定 4文档标准结构
- 所有文件生成逻辑统一封装公共工具
 
2. 前端强制规则
 
- 前端UI全部使用 Vant4
- 生成页必须是「四面板独立预览」
- 必须支持：单文件下载 + 一键打包下载
- 全站响应式、深浅色主题
 
3. 代码规范
 
- 代码分层清晰
- 关键逻辑中文注释
- 接口统一规范
- 所有AI请求异常捕获
- 禁止冗余代码
 
4. 开发顺序
 
严格按照 develop-plan.md 顺序执行，禁止超前开发、禁止乱开发
 
5. 输出格式强制统一
 
无论哪个行业Agent，输出的四个文件名、结构、排版完全统一标准